[package]
name = "scaffold-editor"
version = "0.0.1"
edition = "2024"
publish = false

[lints]
workspace = true

[dependencies]
lexpr = "0.2.7"
line-index = "0.1.2"
markdown-table-formatter = "0.3.0"
serde = { version = "1.0.228", features = ["derive"] }
