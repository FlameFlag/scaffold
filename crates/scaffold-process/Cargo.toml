[package]
name = "scaffold-process"
version = "0.0.1"
edition = "2024"
publish = false

[lints]
workspace = true

[dependencies]
regex = "1.12.4"
thiserror = "2.0.18"
which = { version = "8.0.3", features = ["regex"] }

[dev-dependencies]
tempfile = "3.27.0"
